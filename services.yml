apiVersion: v1
kind: Service
metadata:
  name: e-banking-app
spec:
  type: NodePort
  selector:
    app: e-banking-app
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
      nodePort: 31000
